name: gnested_scrollview
description: 一个自定义的NestedScrollView。解决了NestedScrollView和PageView不能兼容问题。
version: 0.0.1
homepage: https://github.com/xiaofanqingzjj/gnested_scrollview

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter



dev_dependencies:
  flutter_test:
    sdk: flutter

